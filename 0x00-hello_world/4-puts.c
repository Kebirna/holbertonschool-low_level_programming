#include <stdio.h>
#include <string.h>

/*Prints a string with puts function*/

int main(void)
{
	char string[40];

	strcpy(str, "\"Programming is like building a multilingual puzzle");
	puts(string);
	return (0);
}
